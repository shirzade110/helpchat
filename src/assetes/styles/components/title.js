import EStyleSheet from 'react-native-extended-stylesheet';

export const titleStyle = EStyleSheet.create({
  container: {
    marginTop: 80,
    marginBottom: 30,
  },
  title: {
    fontSize: 24,
    color: '$colorTitle',
  },
});
